export type { Database, Json } from "./types/database";
export type { QRPayload } from "./types/qr";
export type {
  AttendanceStatus,
  MarkedBy,
  VerificationResult,
  AttendanceSubmission,
} from "./types/attendance";
export {
  GEOFENCE_DEFAULT_RADIUS_METERS,
  WIFI_MIN_SIGNAL_DBM,
  QR_ROTATION_INTERVAL_MS,
  QR_EXPIRY_SECONDS,
  CLASS_TIME_BUFFER_MINUTES,
  DAYS_OF_WEEK,
} from "./constants";
